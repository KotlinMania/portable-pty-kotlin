# Port targets — `portable-pty-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `portable-pty` that codex-rs uses, and therefore the minimum subset that `portable-pty-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-98] (rank `152`, tier `direct`, status `To Do`)

**Cargo package name**: `portable-pty`
**Rust module name (in `use` statements)**: `portable_pty`

## Summary

- **11** distinct symbols imported by codex-rs
- **16** total `use` statements
- **5** codex source files affected
- **1** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `portable_pty::MasterPty`

- **2 `use` site(s)** across **1 codex member crate(s)**: `codex-utils-pty`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `utils/pty/src/process.rs:10` (`codex-utils-pty`) — `use portable_pty::MasterPty`
- `utils/pty/src/win/conpty.rs:26` (`codex-utils-pty`) — `use portable_pty::MasterPty`

### `portable_pty::PtySize`

- **2 `use` site(s)** across **1 codex member crate(s)**: `codex-utils-pty`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `utils/pty/src/process.rs:11` (`codex-utils-pty`) — `use portable_pty::PtySize`
- `utils/pty/src/win/conpty.rs:28` (`codex-utils-pty`) — `use portable_pty::PtySize`

### `portable_pty::SlavePty`

- **2 `use` site(s)** across **1 codex member crate(s)**: `codex-utils-pty`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `utils/pty/src/process.rs:12` (`codex-utils-pty`) — `use portable_pty::SlavePty`
- `utils/pty/src/win/conpty.rs:30` (`codex-utils-pty`) — `use portable_pty::SlavePty`

### `portable_pty::cmdbuilder::CommandBuilder`

- **2 `use` site(s)** across **1 codex member crate(s)**: `codex-utils-pty`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `utils/pty/src/win/conpty.rs:31` (`codex-utils-pty`) — `use portable_pty::cmdbuilder::CommandBuilder`
- `utils/pty/src/win/psuedocon.rs:30` (`codex-utils-pty`) — `use portable_pty::cmdbuilder::CommandBuilder`

### `portable_pty::Child`

- **2 `use` site(s)** across **1 codex member crate(s)**: `codex-utils-pty`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `utils/pty/src/win/conpty.rs:25` (`codex-utils-pty`) — `use portable_pty::Child`
- `utils/pty/src/win/mod.rs:31` (`codex-utils-pty`) — `use portable_pty::Child`

### `portable_pty::CommandBuilder`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-utils-pty`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `utils/pty/src/pty.rs:24` (`codex-utils-pty`) — `use portable_pty::CommandBuilder`

### `portable_pty::native_pty_system`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-utils-pty`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `utils/pty/src/pty.rs:26` (`codex-utils-pty`) — `use portable_pty::native_pty_system`

### `portable_pty::ChildKiller`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-utils-pty`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `utils/pty/src/win/mod.rs:32` (`codex-utils-pty`) — `use portable_pty::ChildKiller`

### `portable_pty::ExitStatus`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-utils-pty`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `utils/pty/src/win/mod.rs:33` (`codex-utils-pty`) — `use portable_pty::ExitStatus`

### `portable_pty::PtyPair`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-utils-pty`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `utils/pty/src/win/conpty.rs:27` (`codex-utils-pty`) — `use portable_pty::PtyPair`

### `portable_pty::PtySystem`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-utils-pty`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `utils/pty/src/win/conpty.rs:29` (`codex-utils-pty`) — `use portable_pty::PtySystem`
