# Rust callers — `portable-pty-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `portable-pty` | `portable_pty` | 16 | 1 | 1 | 16 |

## `portable-pty`

### Inbound symbols (11 distinct, 16 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `portable-pty`.

#### `portable_pty::Child` — 2 site(s) (2 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-utils-pty` `src/win/conpty.rs:25` — `use portable_pty::Child`
- ✅ `codex-kotlin/codex-utils-pty` `src/win/mod.rs:31` — `use portable_pty::Child`

#### `portable_pty::MasterPty` — 2 site(s) (2 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-utils-pty` `src/process.rs:10` — `use portable_pty::MasterPty`
- ✅ `codex-kotlin/codex-utils-pty` `src/win/conpty.rs:26` — `use portable_pty::MasterPty`

#### `portable_pty::PtySize` — 2 site(s) (2 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-utils-pty` `src/process.rs:11` — `use portable_pty::PtySize`
- ✅ `codex-kotlin/codex-utils-pty` `src/win/conpty.rs:28` — `use portable_pty::PtySize`

#### `portable_pty::SlavePty` — 2 site(s) (2 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-utils-pty` `src/process.rs:12` — `use portable_pty::SlavePty`
- ✅ `codex-kotlin/codex-utils-pty` `src/win/conpty.rs:30` — `use portable_pty::SlavePty`

#### `portable_pty::cmdbuilder::CommandBuilder` — 2 site(s) (2 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-utils-pty` `src/win/conpty.rs:31` — `use portable_pty::cmdbuilder::CommandBuilder`
- ✅ `codex-kotlin/codex-utils-pty` `src/win/psuedocon.rs:30` — `use portable_pty::cmdbuilder::CommandBuilder`

#### `portable_pty::ChildKiller` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-utils-pty` `src/win/mod.rs:32` — `use portable_pty::ChildKiller`

#### `portable_pty::CommandBuilder` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-utils-pty` `src/pty.rs:24` — `use portable_pty::CommandBuilder`

#### `portable_pty::ExitStatus` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-utils-pty` `src/win/mod.rs:33` — `use portable_pty::ExitStatus`

#### `portable_pty::PtyPair` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-utils-pty` `src/win/conpty.rs:27` — `use portable_pty::PtyPair`

#### `portable_pty::PtySystem` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-utils-pty` `src/win/conpty.rs:29` — `use portable_pty::PtySystem`

#### `portable_pty::native_pty_system` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-utils-pty` `src/pty.rs:26` — `use portable_pty::native_pty_system`
