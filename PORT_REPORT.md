# Port report — `portable-pty-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **1** member crate(s): `portable-pty`
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 16 `use` sites from 1 other repos
- **Outbound**: 53 `use` sites to 8 other repos
- **Unresolved external**: 4 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `portable-pty` | `portable_pty` | 16 | 8 | COD-98 | 152 | To Do |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (16 edges, 1 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `codex-kotlin` | `codex-utils-pty` | 16 | ✅ |

### Symbol-level demand (11 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `portable_pty::Child` | 2 | 2 | 0 | 1 |
| `portable_pty::MasterPty` | 2 | 2 | 0 | 1 |
| `portable_pty::PtySize` | 2 | 2 | 0 | 1 |
| `portable_pty::SlavePty` | 2 | 2 | 0 | 1 |
| `portable_pty::cmdbuilder::CommandBuilder` | 2 | 2 | 0 | 1 |
| `portable_pty::ChildKiller` | 1 | 1 | 0 | 1 |
| `portable_pty::CommandBuilder` | 1 | 1 | 0 | 1 |
| `portable_pty::ExitStatus` | 1 | 1 | 0 | 1 |
| `portable_pty::PtyPair` | 1 | 1 | 0 | 1 |
| `portable_pty::PtySystem` | 1 | 1 | 0 | 1 |
| `portable_pty::native_pty_system` | 1 | 1 | 0 | 1 |

#### Per-symbol detail (top 11 symbols)

##### `portable_pty::Child` (2 sites, 2 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-utils-pty` `src/win/conpty.rs:25` — `use portable_pty::Child`
- `codex-kotlin/codex-utils-pty` `src/win/mod.rs:31` — `use portable_pty::Child`

##### `portable_pty::MasterPty` (2 sites, 2 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-utils-pty` `src/process.rs:10` — `use portable_pty::MasterPty`
- `codex-kotlin/codex-utils-pty` `src/win/conpty.rs:26` — `use portable_pty::MasterPty`

##### `portable_pty::PtySize` (2 sites, 2 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-utils-pty` `src/process.rs:11` — `use portable_pty::PtySize`
- `codex-kotlin/codex-utils-pty` `src/win/conpty.rs:28` — `use portable_pty::PtySize`

##### `portable_pty::SlavePty` (2 sites, 2 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-utils-pty` `src/process.rs:12` — `use portable_pty::SlavePty`
- `codex-kotlin/codex-utils-pty` `src/win/conpty.rs:30` — `use portable_pty::SlavePty`

##### `portable_pty::cmdbuilder::CommandBuilder` (2 sites, 2 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-utils-pty` `src/win/conpty.rs:31` — `use portable_pty::cmdbuilder::CommandBuilder`
- `codex-kotlin/codex-utils-pty` `src/win/psuedocon.rs:30` — `use portable_pty::cmdbuilder::CommandBuilder`

##### `portable_pty::ChildKiller` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-utils-pty` `src/win/mod.rs:32` — `use portable_pty::ChildKiller`

##### `portable_pty::CommandBuilder` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-utils-pty` `src/pty.rs:24` — `use portable_pty::CommandBuilder`

##### `portable_pty::ExitStatus` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-utils-pty` `src/win/mod.rs:33` — `use portable_pty::ExitStatus`

##### `portable_pty::PtyPair` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-utils-pty` `src/win/conpty.rs:27` — `use portable_pty::PtyPair`

##### `portable_pty::PtySystem` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-utils-pty` `src/win/conpty.rs:29` — `use portable_pty::PtySystem`

##### `portable_pty::native_pty_system` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-utils-pty` `src/pty.rs:26` — `use portable_pty::native_pty_system`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

| target repo | use sites | distinct symbols | target packages | Jira | rank | status |
|---|---:|---:|---|---|---:|---|
| `libc-kotlin` | 3 | 2 | `libc` | COD-75 | 3 | To Do |
| `lazy-static-kotlin` | 1 | 1 | `lazy_static` | COD-74 | 27 | To Do |
| `futures-kotlin` | 1 | 1 | `futures` | COD-54 | 32 | To Do |
| `anyhow-kotlin` | 14 | 5 | `anyhow` | COD-10 | 47 | To Do |
| `winapi-kotlin` | 20 | 15 | `winapi` | COD-189 | 128 | To Do |
| `shared-library-kotlin` | 1 | 1 | `shared_library` | COD-136 | 149 | To Do |
| `filedescriptor-kotlin` | 11 | 7 | `filedescriptor` | COD-52 | 150 | To Do |
| `serde-kotlin` | 2 | 1 | `serde_derive` | — | — | — |

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

| package | edges | distinct symbols | importing members in this repo |
|---|---:|---:|---|
| `nix` | 17 | 12 | `portable-pty` |
| `winreg` | 6 | 6 | `portable-pty` |
| `serial2` | 5 | 5 | `portable-pty` |
| `downcast-rs` | 4 | 2 | `portable-pty` |
